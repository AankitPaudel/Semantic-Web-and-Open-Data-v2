# football-data.org — free tier API key
# Register at https://www.football-data.org/client/register
FOOTBALL_DATA_KEY=your_api_key_here

# Server port
PORT=3001

# Optional: set to "true" to force mock data (useful for demos without API keys)
USE_MOCK_DATA=false
